name: opendata

services:
  labelct:
    build:
      dockerfile: Dockerfile.label.local
    ports:
      - 55432:55432
    extra_hosts:
      - 'host.docker.internal:host-gateway'
    volumes:
      - .:/home/node
    stdin_open: true
